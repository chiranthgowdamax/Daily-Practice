# Age Validator with Logical Operators 🎂

A Python program that checks if a person is eligible to join a club using age validation with logical operators.

## Features
- Takes age as integer input
- Validates eligibility using logical OR operator
- Provides clear feedback on club eligibility
- Handles special case (16 years old) alongside standard adult threshold

## Concepts Used
- Integer input and type conversion
- Logical operator (`or`) for alternative conditions
- Comparison operators (`>=`, `==`)
- Conditional statements (`if-else`)
- Variable assignment

## How to Run
```bash
python age_validator.py
