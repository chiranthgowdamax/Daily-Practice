# Scholarship Eligibility Checker with Complex Conditions 🧩

A Python program that determines scholarship eligibility based on GPA and volunteer hours using exclusive OR logic.

## Features
- Takes GPA (0.0-4.0) and volunteer hours as inputs
- Implements exclusive OR (XOR) logic using combined operators
- Evaluates scholarship criteria requiring exactly one qualifying condition
- Provides clear approval or denial decisions

## Concepts Used
- Float and integer input handling
- Combined logical operators (`and`, `or`, `not`)
- Comparison operators (`>=`)
- Exclusive OR (XOR) logic implementation
- Conditional statements (`if-else`)
- Complex boolean expressions

## How to Run
```bash
python scholarship_checker.py
