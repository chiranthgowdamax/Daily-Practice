# Password Length Validator 📏

A Python program that checks if a password meets minimum length requirements using string length functions.

## Features
- Accepts password string as input
- Validates minimum length requirement of 8 characters
- Uses `len()` function to count characters
- Provides clear acceptance or rejection feedback

## Concepts Used
- String input handling
- `len()` function for string length
- Comparison operators (`>=`)
- Conditional statements (`if-else`)
- Variable assignment

## How to Run
```bash
python password_validator.py
