# Phone Number Validator 🔢

A Python program that validates phone numbers by checking if they contain only digits using string methods.

## Features
- Accepts phone number as string input
- Validates that all characters are numeric digits (0-9)
- Rejects numbers containing letters, spaces, or special characters
- Provides clear validation feedback

## Concepts Used
- String input handling
- `.isdigit()` method for numeric validation
- Conditional statements (`if-else`)
- String method application
- Variable assignment

## How to Run
```bash
python phone_validator.py
