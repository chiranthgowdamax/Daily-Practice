# Weather Activity Advisor with Combined Logical Operators 🎯

A Python program that determines whether weather conditions are suitable for outdoor activities using combined logical operators.

## Features
- Takes temperature and rain status as inputs
- Evaluates multiple conditions simultaneously using `and` and `not`
- Provides clear activity recommendations
- Handles both numerical and string input validation

## Concepts Used
- Integer and string input handling
- Combined logical operators (`and`, `not`)
- Comparison operators (range checking with `>=` and `<=`)
- String comparison and case handling
- Conditional statements (`if-else`)
- Compound boolean expressions

## How to Run
```bash
python weather_advisor.py
