# Number Range Checker 🔢

A Python program that checks if a number falls within a specific range using logical operators.

## Features
- Accepts any integer as input
- Validates if the number is between 10 and 20 (exclusive)
- Uses logical AND operator to combine two conditions
- Provides simple "Good number" or "Bad number" feedback

## Concepts Used
- Integer input and type conversion
- Logical AND operator
- Comparison operators (`>`, `<`)
- Conditional statements (`if-else`)
- Variable assignment

## How to Run
```bash
python number_range_checker.py
